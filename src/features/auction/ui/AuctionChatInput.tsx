import React from "react";
import {Input} from "@shared/components/ui/input.tsx";
import StompClient from "@/features/auction/ui/StompClient.tsx";

const AuctionChatInput = () => {
    return (
        <>
            <Input/>
        </>
    );
};

export default AuctionChatInput;
